{
  "name": "bot",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node --env-file-if-exists=.env -r ts-node/register src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file-if-exists=.env dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "grammy": "^1.38.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
